[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "model-pipeline"
version = "0.1.0"
description = "SavVio ML Training Pipeline"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
