# =============================================================================
# SavVio Data Pipeline - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git - it contains sensitive credentials
# =============================================================================

# -----------------------------------------------------------------------------
# GCP Configuration
# -----------------------------------------------------------------------------
GCP_PROJECT_ID=your-gcp-project-id
# Path to your service account JSON key file (for local development)
# Leave empty to use Application Default Credentials (ADC) in GCP environments
GCP_CREDENTIALS_PATH=/path/to/your/service-account-key.json

# -----------------------------------------------------------------------------
# GCS Storage Configuration
# -----------------------------------------------------------------------------
GCS_BUCKET_NAME=savvio-data-bucket

# GCS folder structure (with trailing slashes)
GCS_RAW_PATH=raw/
GCS_PROCESSED_PATH=processed/
GCS_FEATURES_PATH=features/
GCS_VALIDATED_PATH=validated/

# Specific blob paths for raw data
FINANCIAL_BLOB=raw/financial_data.csv
PRODUCT_BLOB=raw/product_data.json
REVIEW_BLOB=raw/review_data.json

# -----------------------------------------------------------------------------
# Local File Paths
# -----------------------------------------------------------------------------
# Base directory for storing downloaded/processed data locally
DATA_DIR=data

# -----------------------------------------------------------------------------
# API Configuration (Production)
# -----------------------------------------------------------------------------
# Base URL for SavVio API (used in production environment)
API_BASE_URL=https://api.savvio.com/v1
# API authentication key
API_KEY=your-api-key-here
# API request timeout in seconds
API_TIMEOUT=30

# Specific API endpoints (optional - will be constructed from API_BASE_URL if not provided)
# FINANCIAL_API_ENDPOINT=https://api.savvio.com/v1/financial
# PRODUCT_API_ENDPOINT=https://api.savvio.com/v1/products
# REVIEW_API_ENDPOINT=https://api.savvio.com/v1/reviews

# -----------------------------------------------------------------------------
# Environment Configuration
# -----------------------------------------------------------------------------
# Environment: dev, staging, or prod
ENVIRONMENT=dev

# Data source: gcs or api
# If not set, will be automatically determined based on ENVIRONMENT
# dev -> gcs, prod -> api
# DATA_SOURCE=gcs

# -----------------------------------------------------------------------------
# Pipeline Configuration
# -----------------------------------------------------------------------------
# Maximum percentage of missing values allowed per column (0.0 to 1.0)
MAX_MISSING_VALUES_PCT=0.1

# Minimum number of records required for pipeline to proceed
MIN_RECORDS_REQUIRED=100

# Comma-separated list of columns representing monthly income
MONTHLY_INCOME_COLS=income,salary

# Comma-separated list of columns representing monthly expenses
MONTHLY_EXPENSE_COLS=rent,bills,subscriptions

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Directory for storing log files
LOG_DIR=logs

# -----------------------------------------------------------------------------
# DVC Configuration
# -----------------------------------------------------------------------------
# DVC remote name
DVC_REMOTE_NAME=gcs

# DVC remote URL (GCS bucket path for storing DVC cache)
# DVC_REMOTE_URL=gs://savvio-data-bucket/dvc-store

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file to .env in the same directory:
#    cp .env.example .env
#
# 2. Fill in your actual values in .env
#
# 3. Make sure .env is in .gitignore (it should be!)
#
# 4. For GCP authentication, either:
#    - Set GCP_CREDENTIALS_PATH to your service account key file, OR
#    - Use `gcloud auth application-default login` for ADC
#
# 5. Create your GCS bucket:
#    gsutil mb -p your-gcp-project-id gs://savvio-data-bucket
#
# 6. Verify configuration by running:
#    python config.py
# =============================================================================